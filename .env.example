# TowerVision AI Environment Config
APP_NAME=TowerVision AI
DEBUG=True
PORT=8000
HOST=0.0.0.0

# Quality Gate Thresholds
MIN_LAPLACIAN_BLUR_SCORE=80.0
MIN_BRIGHTNESS=40.0
MAX_BRIGHTNESS=220.0
MIN_CONTRAST_RMS=25.0

# YOLO Detection
# Defaults to the bundled backend/ML Model.zip checkpoint. Set YOLO_MODEL_PATH to override it.
DETECTION_CONFIDENCE_THRESHOLD=0.35

# Set true when served over HTTPS in production.
AUTH_COOKIE_SECURE=False
